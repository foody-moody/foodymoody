spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodymoody?TC_REUSABLE=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: bono
    password: 1111
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
